CREATE TABLE appTransaction (
    transferType TEXT,
    totalAmount Text,
    description TEXT,
    quantity TEXT,
    date TEXT,
    identity TEXT
);

insertTransaction:
INSERT INTO appTransaction(transferType, totalAmount, description, quantity, date, identity)
VALUES( ?, ?, ?, ?, ?,?);

getAppTransactions:
SELECT *
FROM appTransaction;